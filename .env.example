# Optional: default API key fallback for Gemini models
VITE_GEMINI_API_KEY=

# Optional: default API key fallback for OpenAI-compatible models
VITE_OPENAI_API_KEY=

# Optional: generic fallback used when provider-specific key is absent
VITE_API_KEY=
